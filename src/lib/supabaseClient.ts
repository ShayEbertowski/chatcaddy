// import { PostgrestClient } from '@supabase/postgrest-js';
// import 'react-native-url-polyfill/auto';
// import Constants from 'expo-constants';
// import { useAuthStore } from '../stores/useAuthStore';

// const SUPABASE_URL = Constants.expoConfig?.extra?.SUPABASE_URL;
// const SUPABASE_ANON_KEY = Constants.expoConfig?.extra?.SUPABASE_ANON_KEY;

// // Factory function that builds client with correct token
// export function createSupabaseClient() {
//     const token = useAuthStore.getState().accessToken;

//     const authHeader = token
//         ? `Bearer ${token}`     // ✅ use user's access token if available
//         : `Bearer ${SUPABASE_ANON_KEY}`; // fallback for unauthenticated access

//     return new PostgrestClient(`${SUPABASE_URL}/rest/v1`, {
//         headers: {
//             Authorization: authHeader,
//         },
//     });
// }



// TODO: ⚠️ RLS is DISABLED on prompts table. Secure before production.
// TODO: ⚠️ RLS is DISABLED on prompts table. Secure before production.
// TODO: ⚠️ RLS is DISABLED on prompts table. Secure before production.
// TODO: ⚠️ RLS is DISABLED on prompts table. Secure before production.
// TODO: ⚠️ RLS is DISABLED on prompts table. Secure before production.
// TODO: ⚠️ RLS is DISABLED on prompts table. Secure before production.
// TODO: ⚠️ RLS is DISABLED on prompts table. Secure before production.
// TODO: ⚠️ RLS is DISABLED on prompts table. Secure before production.
