import { TText } from './types';

export const typography: {
    heading: TText;
    body: TText;
    small: TText;
} = {
    heading: {
        fontSize: 18,
        fontWeight: '600',
    },
    body: {
        fontSize: 14,
        fontWeight: '400',
    },
    small: {
        fontSize: 12,
        fontWeight: '400',
    },
};
