import { View, Text } from 'react-native';

export default function StackHome() {
    return (
        <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
            <Text>This is your stack entry point.</Text>
        </View>
    );
}
